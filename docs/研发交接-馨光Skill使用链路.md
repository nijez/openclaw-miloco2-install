# 研发交接：馨光 Skill 使用链路

更新时间：2026-07-01

## 结论

部署器职责已收敛为安装与安装诊断：

- Miloco 2.0 安装；
- 馨光 Skill 下载、安装、刷新；
- 安装状态检查；
- 重复运行幂等与修复安装；
- 安装失败诊断；
- 敏感信息不外泄。

下面内容不属于部署器职责，后续应由研发侧维护。

## 移交研发的内容

- preview / confirm / execute / restore；
- request_id 状态机；
- 真实执行许可；
- 目标设备锁定；
- `/api/generate` 调用；
- `/api/devices` 目标设备匹配；
- 设备 offline 处理；
- L6-B / L6-BR / L6-R / L6-S / L6-T；
- 真实控灯验证；
- 灯光执行安全策略；
- 馨光 Skill 内部提示词、路由规则和控灯工具设计。

## 对部署器的要求

部署器以后只保证：

- Miloco 2.0 可安装、可验证；
- 馨光 Skill 可下载、可安装、可刷新；
- Skill 文件版本可检查；
- 灯光服务安装依赖可检查；
- 重复运行不破坏已有安装；
- 失败时给出安装层原因；
- 不继续承担自然语言控灯、目标设备锁定和真实执行验证。

## 本轮越界改动处理

上一轮 L6-T3 本地候选涉及：

- `xinguang-lock-target-device`
- `xinguang-verify-target-device`

根据当前边界，这两项属于目标设备锁定 / offline 排查，不再作为部署器交付物推进。本轮已停止验证，未 push，并从本地工作区撤回未发布改动。

## Skill 文件处理原则

部署器只安装研发交付的 Skill 文件，不负责重写 Skill 使用规则。

如果当前 Skill 文件中仍包含业务使用链路、控灯状态机或设备安全策略，应由研发侧确认基线后再更新；部署器不再凭空清理或重写 Skill 内容。
